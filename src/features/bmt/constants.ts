import type { UnitBmtFilters } from "./types";

export const bmtEndpoint = "/v1/auth/instansi";
export const publicBmtEndpoint = "/v1/instansi";

export const defaultBmtFilters: UnitBmtFilters = {
  search: "",
};
